// import { fromJS } from 'immutable';
// import { makeSelectRegistrationPageDomain } from '../selectors';

// const selector = makeSelectRegistrationPageDomain();

describe('makeSelectRegistrationPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
