// import { fromJS } from 'immutable';
// import { makeSelectAchievementsPageDomain } from '../selectors';

// const selector = makeSelectAchievementsPageDomain();

describe('makeSelectAchievementsPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
