// import { fromJS } from 'immutable';
// import { makeSelectCreateRewardDomain } from '../selectors';

// const selector = makeSelectCreateRewardDomain();

describe('makeSelectCreateRewardDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
