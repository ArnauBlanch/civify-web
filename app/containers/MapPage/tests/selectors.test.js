// import { fromJS } from 'immutable';
// import { makeSelectMapPageDomain } from '../selectors';

// const selector = makeSelectMapPageDomain();

describe('makeSelectMapPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
