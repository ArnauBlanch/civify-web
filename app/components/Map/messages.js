/*
 * SearchBox Messages
 *
 * This contains all the text for the SearchBox component.
 */
import { defineMessages } from 'react-intl';

export default defineMessages({
  searchPlace: {
    id: 'app.components.Map.search-place',
    defaultMessage: 'Search a place',
  },
});
